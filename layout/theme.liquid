<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#2563eb">
  
  <title>{{ page_title }}</title>
  <meta name="description" content="{{ page_description | escape }}">
  
  <!-- Preload fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Shopify required -->
  {{ content_for_header }}
  
  <!-- Theme CSS -->
  {{ 'theme.css' | asset_url | stylesheet_tag }}
  
  <!-- Enhanced Custom CSS -->
  <style>
    :root {
      --primary-50: #eff6ff;
      --primary-100: #dbeafe;
      --primary-200: #bfdbfe;
      --primary-300: #93c5fd;
      --primary-400: #60a5fa;
      --primary-500: #3b82f6;
      --primary-600: #2563eb;
      --primary-700: #1d4ed8;
      --primary-800: #1e40af;
      --primary-900: #1e3a8a;
      --accent-50: #fffbeb;
      --accent-100: #fef3c7;
      --accent-200: #fde68a;
      --accent-300: #fcd34d;
      --accent-400: #fbbf24;
      --accent-500: #f59e0b;
      --accent-600: #d97706;
      --accent-700: #b45309;
      --accent-800: #92400e;
      --accent-900: #78350f;
      --gray-50: #f9fafb;
      --gray-100: #f3f4f6;
      --gray-200: #e5e7eb;
      --gray-300: #d1d5db;
      --gray-400: #9ca3af;
      --gray-500: #6b7280;
      --gray-600: #4b5563;
      --gray-700: #374151;
      --gray-800: #1f2937;
      --gray-900: #111827;
    }
    
    * {
      box-sizing: border-box;
    }
    
    html {
      scroll-behavior: smooth;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 0;
      padding: 0;
      line-height: 1.6;
      color: var(--gray-900);
      background-color: #ffffff;
      cursor: url('data:image/svg+xml;utf8,<svg height="32" width="32" xmlns="http://www.w3.org/2000/svg"><circle cx="16" cy="16" r="8" fill="%230ea5e9" opacity="0.7"/></svg>') 16 16, auto;
      transition: cursor 0.2s;
    }
    
    /* Enhanced Gradients */
    .bg-animated-gradient {
      background: linear-gradient(270deg, var(--primary-600), var(--accent-500), var(--primary-700));
      background-size: 600% 600%;
      animation: gradientMove 8s ease infinite;
    }
    
    .bg-hero-gradient {
      background: linear-gradient(135deg, var(--primary-600) 0%, var(--primary-800) 50%, var(--accent-600) 100%);
    }
    
    .bg-card-gradient {
      background: linear-gradient(145deg, #ffffff 0%, var(--gray-50) 100%);
    }
    
    .text-gradient-primary {
      background: linear-gradient(135deg, var(--primary-600) 0%, var(--accent-500) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    
    /* Enhanced Buttons */
    .btn-primary {
      background: linear-gradient(135deg, var(--primary-600) 0%, var(--primary-700) 100%);
      color: white;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
      transform: translateY(0);
      box-shadow: 0 4px 6px -1px rgba(37, 99, 235, 0.1);
      min-width: 180px;
      min-height: 48px;
      font-size: 1.1rem;
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px -3px rgba(37, 99, 235, 0.2);
    }
    
    .btn-secondary {
      background: white;
      color: var(--primary-600);
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      border: 2px solid var(--primary-600);
      cursor: pointer;
      transition: all 0.3s ease;
      transform: translateY(0);
      min-width: 180px;
      min-height: 48px;
      font-size: 1.1rem;
    }
    
    .btn-secondary:hover {
      background: var(--primary-600);
      color: white;
      transform: translateY(-2px);
    }
    
    .btn-accent {
      background: linear-gradient(135deg, var(--accent-500) 0%, var(--accent-600) 100%);
      color: white;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
      transform: translateY(0);
      box-shadow: 0 4px 6px -1px rgba(245, 158, 11, 0.1);
      min-width: 180px;
      min-height: 48px;
      font-size: 1.1rem;
    }
    
    .btn-accent:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px -3px rgba(245, 158, 11, 0.2);
    }
    
    /* Enhanced Cards */
    .card {
      background: white;
      border-radius: 1rem;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      transform: translateY(0);
      overflow: hidden;
    }
    
    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    }
    
    /* Enhanced Animations */
    .animate-fade-in {
      animation: fadeIn 0.6s ease-out;
    }
    
    .animate-slide-up {
      animation: slideUp 0.6s ease-out;
    }
    
    .animate-bounce-gentle {
      animation: bounceGentle 2s infinite;
    }
    
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    
    @keyframes slideUp {
      from { 
        opacity: 0;
        transform: translateY(30px);
      }
      to { 
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @keyframes bounceGentle {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    
    /* Enhanced Container */
    .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    
    @media (min-width: 640px) {
      .container {
        padding: 0 1.5rem;
      }
    }
    
    @media (min-width: 1024px) {
      .container {
        padding: 0 2rem;
      }
    }
    
    /* Custom Cursor for Interactive Elements */
    @media (hover: hover) {
      a, button, .cursor-pointer {
        cursor: url('data:image/svg+xml;utf8,<svg height="32" width="32" xmlns="http://www.w3.org/2000/svg"><circle cx="16" cy="16" r="12" fill="%23f59e0b" opacity="0.8"/></svg>') 16 16, pointer;
        transition: cursor 0.2s;
      }
    }
    
    /* Enhanced Typography */
    h1, h2, h3, h4, h5, h6 {
      font-family: 'Poppins', system-ui, sans-serif;
      font-weight: 600;
      line-height: 1.2;
    }
    
    /* Enhanced Focus States */
    .focus-ring:focus {
      outline: none;
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
    }
  </style>
</head>

<body>
  {% section 'header' %}
  
  <main role="main" id="MainContent">
    {{ content_for_layout }}
  </main>
  
  {% section 'footer' %}
  
  <!-- Shopify required -->
  {{ content_for_footer }}
  
  <!-- Theme JS -->
  {{ 'theme.js' | asset_url | script_tag }}
</body>
</html>
